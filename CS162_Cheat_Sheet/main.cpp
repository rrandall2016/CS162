#include <iostream>
#include <cmath>
#include <string>
#include "Circle1.h"
#include "Circle2.h"
#include "Book1.h"
#include "Point1.h"
#include "Point2.h"

using namespace std;

//This is a dummy class which you can use as a template in case you need to set up declaration and implementation all in one place.
/*class Circle3
{
//Public: All data fields, constructors, and functions can be accessed from the objects of the class.
public:
    //Circle3() is a "no-argument constructor".
    Circle3() {
        radius = 1;
    }

    Circle3(double newRadius) {
        radius = newRadius;
    }

    double getArea() {
        return radius * radius * 3.14;
    }

    double getCircumference() {
        return 2 * radius * 3.14;
    }

    void setRadius(double newRadius) {
        radius = newRadius;
    }

private:
    double radius;
};*/

int main()
{
    //Comment out the weeks you do not want to execute.
//    executeWeekOne();
//    executeWeekTwo();
    executeWeekThree();
//    pointerBasics();
//    dynamicMemory();
    return (0);
}
